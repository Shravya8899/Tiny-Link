datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client-js"
}

model Link {
  id        String   @id @default(cuid())
  slug      String   @unique
  url       String
  createdAt DateTime @default(now())
}


